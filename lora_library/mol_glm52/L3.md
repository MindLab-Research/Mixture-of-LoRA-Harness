---
id: L3
level: L3
task: ui/a2ui
adapter_name: L3
source_path: /path/to/lora_checkpoints/L3
dataset:
priority: 92
---

# L3 UI / A2UI

## Summary
A2UI, GenUI, and TSX/React UI generation. Choose L3 for the Current surface context / Compose the best A2UI response user wrapper or an explicit request for a rendered app screen, card, dashboard, form, tracker, calculator, game, component, or interactive surface.

## Router Line
UI4A/A2UI/GenUI runtime surface-protocol execution: text containing Current surface context, surface_id, and Compose the best A2UI response is L3; classify that wrapper as L3 instead of composing its JSON or text_response. Ordinary webpage, HTML/CSS/JavaScript, React, Three.js/WebGL, browser simulation, game, or visualization implementation is L2.

## Description
Specialist LoRA for A2UI and UI-generation workflows. Use this route when the user asks the model to produce or modify an actual interface, app screen, frontend component, layout, design system surface, interaction state, or UI artifact.

## Routing Rules
- Choose L3 when the request is to build, generate, modify, or evaluate a UI, frontend page, app screen, dashboard, component, layout, style, interaction, or visual product surface.
- Choose L3 when the user explicitly mentions A2UI, agent-to-UI, UI generation, GenUI, frontend implementation, component state, or screen layout.
- Prefer L3 over L0 for actual UI construction; stay on L0 for general UI advice or copy-only critique.
- Prefer L2 over L3 when the task is primarily repository repair, tests, scripts, or a local artifact pipeline rather than UI generation.
- Choose L3 when the user turn contains the Current surface context / Compose the best A2UI response wrapper, even when its topic is a meal, workout, trip, recommendation, study plan, tracker, timer, calculator, or game.

## Strong Signals
A2UI, a2ui, agent-to-UI, agent to UI, GenUI, genui, UI generation, generate UI, build a UI, create a UI, app screen, screen layout, frontend component, React component, Vue component, dashboard layout, interface layout, visual interaction, interaction state, component states, design system, wireframe, mockup, page layout, web app screen, mobile app screen, Current surface context, surface context, surface_id, Compose the best A2UI response, A2UI response, text_response, beginRendering, surfaceUpdate, dataModelUpdate, deleteSurface
, Execution Contract, rendered interactive UI, display_tsx, raw TSX, export default function App, $macaron/ui, genui check, rendered card, interactive card, tracker UI, timer UI, calculator UI, game UI

## Positive Signals
frontend UI, user interface, UI screen, app screen, dashboard screen, dashboard layout, interface layout, compact UI layout, UI layout, frontend component, React settings page, React component, Vue component, Svelte component, form component, modal dialog, sidebar navigation, tabbed interface, table view, chart panel, responsive layout, CSS styling, Tailwind CSS, HTML page, visual design, product surface, interaction state, hover state, empty state, loading state, validation errors, disabled state, save toolbar
, recommendation card, meal planner screen, workout tracker, study planner, mood tracker, itinerary screen, shopping comparison card, progress dashboard, focus timer, interactive calculator, playable game surface

## Negative Signals
Use Pi's available tools, BENCHMARK ISSUE, Harbor SWE-Bench, repository issue, source-code fix, customer service agent, v3p_res_, Rho-Bank, credit card, VitaBench, SYN_OTA, 火车票, 酒店, 外卖, OpenClaw, input/, output/, deterministic artifacts, run a scan
, scripts/, scraper, scrape, download, local workspace, workspace, local file, read files

## Examples
- Build a responsive dashboard screen with filters, a data table, and empty/loading states. => L3
- Generate a React component for a settings page with tabs and validation states. => L3
- Create an A2UI app screen for reviewing routed LoRA requests. => L3
